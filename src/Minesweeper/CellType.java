package Minesweeper;

public enum CellType {

    ONE(), TWO(), THREE(), FOUR(),
    FIVE(), SIX(), SEVEN(), EIGHT(),
    UNTAPPED(), BOMB(), FLAG_BOMB(), FLAG_NO_BOMB(),
    EMPTY(), NULL(), SUN();


    CellType() {
    }


}
